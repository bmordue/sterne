<%= form_for(@ship_type) do |f| %>
  <% if @ship_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ship_type.errors.count, "error") %> prohibited this ship_type from being saved:</h2>

      <ul>
      <% @ship_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :attack %><br />
    <%= f.text_field :attack %>
  </div>
  <div class="field">
    <%= f.label :defense %><br />
    <%= f.text_field :defense %>
  </div>
  <div class="field">
    <%= f.label :speed %><br />
    <%= f.text_field :speed %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
