<h1>Listing games</h1>

<table>
  <tr>
    <th>Universe</th>
    <th>Players</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @games.each do |game| %>
  <tr>
    <td><%= game.universe_id %></td>
    <td><%= link_to 'Show', game %></td>
    <td><%= link_to 'Join game', join_game_path(game), :method => :post %></td>
    <td><%= link_to 'Edit', edit_game_path(game) %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Game', new_game_path %>
